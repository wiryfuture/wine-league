From 75167d7e39566706733933b484474b77a6e7e7ce Mon Sep 17 00:00:00 2001
From: Philip Wilk <wiryfuture@gmail.com>
Date: Sun, 19 Dec 2021 21:49:47 +0000
Subject: [PATCH] commit 737fe1f is dead to me

---
 server/fd.c | 9 ++++++---
 1 file changed, 6 insertions(+), 3 deletions(-)

diff --git a/server/fd.c b/server/fd.c
index c9a21186722..8c3247f5a3f 100644
--- a/server/fd.c
+++ b/server/fd.c
@@ -566,7 +566,8 @@ static inline void set_fd_epoll_events( struct fd *fd, int user, int events )
     }
     else if (pollfd[user].fd == -1)
     {
-        ctl = EPOLL_CTL_ADD;
+        /*if (pollfd[user].events) return; */ /* stopped waiting on it, don't restart */
+        ctl = EPOLL_CTL_ADD;   
     }
     else
     {
@@ -664,6 +665,7 @@ static inline void set_fd_epoll_events( struct fd *fd, int user, int events )
     }
     else if (pollfd[user].fd == -1)
     {
+        if (pollfd[user].events) return;  /* stopped waiting on it, don't restart */
         ev[0].flags |= EV_ADD | ((events & POLLIN) ? EV_ENABLE : EV_DISABLE);
         ev[1].flags |= EV_ADD | ((events & POLLOUT) ? EV_ENABLE : EV_DISABLE);
     }
@@ -772,7 +774,8 @@ static inline void set_fd_epoll_events( struct fd *fd, int user, int events )
     }
     else if (pollfd[user].fd == -1)
     {
-        ret = port_associate( port_fd, PORT_SOURCE_FD, fd->unix_fd, events, (void *)user );
+        if (pollfd[user].events) return;  /* stopped waiting on it, don't restart */
+        ret = port_associate( port_fd, PORT_SOURCE_FD, fd->unix_fd, events, (void *)user ); 
     }
     else
     {
@@ -1651,7 +1654,7 @@ void set_fd_events( struct fd *fd, int events )
         pollfd[user].events = POLLERR;
         pollfd[user].revents = 0;
     }
-    else
+    else if (pollfd[user].fd != -1 || !pollfd[user].events)
     {
         pollfd[user].fd = fd->unix_fd;
         pollfd[user].events = events;
-- 
2.33.1

